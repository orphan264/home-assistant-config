- platform: statistics
  entity_id: sensor.spectrumup
  name: stats_spectrum_up
  sampling_size: 300
  max_age:
    days: 7

- platform: statistics
  entity_id: sensor.spectrumdown
  name: stats_spectrum_down
  sampling_size: 300
  max_age:
    days: 7

- platform: statistics
  entity_id: sensor.spectrumping
  name: stats_spectrum_ping
  sampling_size: 300
  max_age:
    days: 7